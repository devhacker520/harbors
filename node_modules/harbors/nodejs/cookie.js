// Generated by CoffeeScript 1.6.3
(function() {
  var harbors;

  harbors = require('../harbors_modules/harbors');

  exports.handle = function(name, value) {
    var p, self, type, _i, _len;
    self = this;
    if (name === void 0) {
      return this.__cookie;
    }
    type = typeof name;
    if (type === "object") {
      for (_i = 0, _len = name.length; _i < _len; _i++) {
        p = name[_i];
        this._cookie.push(p + '=' + name[p]);
      }
    } else if (type === "string") {
      if (typeof value === "string") {
        self._cookie.push(name + '=' + value);
      }
      return this.__cookie[name];
    }
  };

  exports.load = function(string) {
    var i, temp, temp_2, _cookie, _i, _ref;
    temp = string.split(";");
    _cookie = {};
    for (i = _i = _ref = temp.length - 1; _ref <= 0 ? _i <= 0 : _i >= 0; i = _ref <= 0 ? ++_i : --_i) {
      temp_2 = temp[i].split("=");
      temp_2[0] = temp_2[0].replace(/^\s*/, '');
      _cookie[temp_2[0]] = temp_2[1];
    }
    return _cookie;
  };

}).call(this);

/*
//@ sourceMappingURL=cookie.map
*/

var harbors = require('../harbors');

exports.clearCache = function(string){
    for(var p in require.cache){
        if(require.cache[p].id.indexOf(string)>-1){
            harbors.log("Clear require : ", require.cache[p].id);
            delete require.cache[p];
        }
    }
};
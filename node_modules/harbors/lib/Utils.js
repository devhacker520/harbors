/**
 * 工具集合
 */


/**
 * 合并元素a、b
 *
 * @param {Object} a
 * @param {Object} b
 * @return {Object} {a:a,b:b}
 */
exports.merge = function(a, b){
    if (a && b) {
        for (var key in b) {
            a[key] = b[key];
        }
    }
    return a;
};
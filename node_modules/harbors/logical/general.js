/**
 * 合并配置文件
 */
exports.mergeConfig = function(){
    harbors.config = require('../default/config');
    try{
        harbors.config = harbors.utils.merge(harbors.config,require('../../../config/config'));
    }catch(err){
        console.log('User configuration file not found!');
    }
};